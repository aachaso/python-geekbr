number_user = input('Enter the numeral "n" - ')

number_two = int(number_user + number_user)
number_three = int(number_user + number_user + number_user)
number_user = int(number_user)
sum_user = number_user + number_two + number_three

print(f'n + nn + nnn = {sum_user}')
